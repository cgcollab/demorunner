#_ECHO_OFF

# Prep for demo
mkdir -p temp-demorunner
cd temp-demorunner
rm *.sh  # Clean up from previous runs

echo "echo \"Hello, world!\"" > hello.sh
chmod +x hello.sh

# Start demo
clear
#_ECHO_ON

#_ECHO_OFF
echo "Instructions: Press Return to display a scripted command, and again to execute it"
echo "              Type any ad-hoc commands at the prompt"
echo "              Use up/down arrows to navigate command history"
echo "              Use left/right arrows to edit a displayed command"
#_ECHO_ON

#_ECHO_E_# Today is $(date +%A), amirite?

pwd
ls
./hello.sh

#_ECHO_# Ready for a magic trick? I will silently change hello into goodbye!
#_ECHO_OFF
mv hello.sh goodbye.sh
sed -i '' -e 's/Hello, world/Later, alligator/g' goodbye.sh
#_ECHO_ON
ls
./goodbye.sh

#_ECHO_# Questions?
#_ECHO_#
